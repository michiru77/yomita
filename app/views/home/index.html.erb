<!-- 履歴ページの表示 -->
<span id="history_page">
  <% listnumber=0 %>
  <% for listEL in cookies[:image].split('&') do %>
  <% if listEL!=["tmp"] then%>
  <% if listEL!="tmp" then%>
  <img src="<%= listEL %>" width="90px" height="auto" alt="<%= cookies[:ISBN].split('&')[listnumber] %>">
  <% listnumber=listnumber+1 %>
  <% end %>
  <% end %>
  <% end %>
</span>

<% session[:image]%>

<% link_to("履歴消しましょう","/home_history") %>

<!-- セッションによる表示 -->
<% if session[:Receivefruit].blank? then session[:Receivefruit]=[] end %>

<% for num in session[:Receivefruit].length-9..session[:Receivefruit].length-1 do %>
<% if num>=1 then %>
<!--
    <img src="<%= session[:Receivefruit][num] %>" width="90px" height="auto" alt="<%= session[:alt] %>">
    -->
<!--
    <p><%= p session[:alt] %></p>
    -->
<% end %>
<% end %>
<!--
    <% session[:Receivefruit].each do |i| %>
    <img src="<%= i %>" width="90px" height="auto">
    <% end %>
    -->

<% session[:Receivefruit].each do |i|%>
<!--
    <img src="<%= i %>" width="50" height="50">
    -->
<% end %>


<!-- ここからモーダルウィンドウ -->
<div id="modal-content">
  <div id="modal-content-innar">
    <!-- モーダルウィンドウのコンテンツ開始 -->
    <p class="red bold">
      <!--
          あらすじを追加する場合はここに追加する
        -->
      <br /></p>
    <p><a id="modal-close" class="button-link">閉じる</a></p>
  </div>
  <!-- モーダルウィンドウのコンテンツ終了 -->
</div>


<div class="title"></div>
<div class="author"></div>
<div class="center"></div>


<div id="photos_1"></div>
<section id="photos_6"></section>


<!--トップページ読み込み時に使用-->
<div id="top_loading"></div>
<!--下スクロールイベント時に使用-->
<div id="end_loading"></div>

<!--トップページ読み込み時に使用-->
<div id="top_loading"></div>
<!--下スクロールイベント時に使用-->
<div id="end_loading"></div>

<!-- 本の道筋をmouseoverした際ここにそのimg(src値)が追加されます -->
<div id="display_history_img"></div>
